$include <std.fl>

function spark(int argc, str[] argv) -> int {
	printi(1 << 2);
	printi((1 << 2) >> 1);
	printi(1 + (2 * 2 << 3));
	printi(10 | 20);
	printi(10 & 2);
	printi(10 ^ 10);
	printi(~10);
	return 0;
}